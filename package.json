{
  "name": "personal-notes-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "deploy": "npm run build"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "luxon": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^1.1.0",
    "@11ty/eleventy-navigation": "^2.0.0"
  }
}
